global:
  security:
    allowInsecureImages: true

image:
  registry: registry-1.docker.io
  repository: bitnamisecure/redis
  tag: latest

metrics:
    enabled: true
    image:
      registry: registry-1.docker.io
      repository: bitnamisecure/redis-exporter
      tag: latest

auth:
  enabled: true
  password: "test-redis"

master:
  persistence:
    enabled: true
    size: 1Gi
    storageClass: "standard"

replica:
  replicaCount: 2
  persistence:
    enabled: true
    size: 1Gi
    storageClass: "standard"

